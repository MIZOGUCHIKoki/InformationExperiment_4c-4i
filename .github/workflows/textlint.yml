name: run-textlint
on: 
  push:
    branches:
      - master
    paths:
      - '**/**.tex'
  pull_request_target:
    paths:
      - '**/**.tex'
  
jobs:
  run-textlint_WF:
    uses: MIZOGUCHIKoki/ConfigFiles/.github/workflows/textlint_main.yml@output_of_as_text
    secrets:
      gh_token: ${{ secrets.ACCESS_TOKEN }}
